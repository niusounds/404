<?xml version="1.0" encoding="UTF-8"?>
<svg width="800" height="450" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- Soft glow for the woman figure -->
    <filter id="glow">
      <feGaussianBlur stdDeviation="2.5" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
    <!-- Radial vignette overlay for dark edges -->
    <radialGradient id="vignette" cx="0.5" cy="0.35">
      <stop offset="30%" stop-color="transparent"/>
      <stop offset="120%" stop-color="#ff0000"/>
    </radialGradient>
  </defs>

  <!-- Background base -->
  <rect width="800" height="450" fill="#0a0808"/>

  <!-- Night sky -->
  <rect x="130" y="20" width="540" height="190" fill="#0f0926"/>

  <!-- Moon (slightly crescent) -->
  <circle cx="380" cy="95" r="42" fill="#ffe0b5"/>
  <circle cx="405" cy="78" r="38" fill="#120a32" stroke="#160c42" stroke-width="1.5">

  

</svg>

  <!-- Moon crescent (draw dark circle to hide part of light circle) -->
  <circle cx="408" cy="78" r="36"/> 

<!-- Moon crescent effect: draw a dark oval covering the right portion -->
<ellipse cx="415" cy="72" rx="30" ry="36" fill="#0f0a2b"/>

  <!-- Misty clouds -->
  <g opacity="0.35">
    <ellipse cx="80" cy="65" rx="90" ry="14" fill="#2e1a52"/>
    <ellipse cx="700" cy="100" rx="110" ry="18" fill="#3c1e6e"/><path d="M 20,80 Q 80,60 140,90" stroke="none"/>
    <ellipse cx="720" cy="55" rx="75" ry="11" fill="" stroke="none"/></g>

      <!-- Dark clouds / mist wisps at top -->
<g opacity="0.6">
  <path d="M 120,40 Q 300,35 500,55 T 680,35" fill="#241e7a"/>
  <ellipse cx="140" cy="60" rx="80" ry="5"/></g>

  <!-- Dock leading into the water (perspective lines) -->
  <polygon points="180,450 380,255 420,255 620,450" fill="#2e1c1c"/>
  
  <!-- Horizontal plank lines on dock -->
  <line x1="210" y1="440" stroke="#4a3434" stroke-width="1.8"/>
  <line x1="275" y

</p

</svg>

  <!-- Dock planks (perspective lines) -->
  <g clip-path="url(#dock-clip)">
    <!-- Left plank edges -->
    <path d="M 365,450 L 385,270" stroke="#3e2a2a" stroke-linecap="round"></svg>
      
<clipPath id

<!-- Dark water below the dock -->
</rectx="
